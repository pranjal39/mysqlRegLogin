<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login page</title>
    <script>
        function checkLog(form) {
            if (form.em.value === "test@gmail.com" && form.pw.value === "Asdfg123") {
                return true;
            }
            else {
                alert('Wrong Password')
                return false;
            }
        }

    </script>
    <script>
        function validate() {
            // email validation
            const em1 = document.getElementById("em").value;
            const em2 = document.getElementById("em");
            const re_em = /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/;
            if (re_em.test(em1)) {
                em2.style.border = "green solid 3px";
                document.getElementById("errEm").innerHTML = ":)";
            }
            else {
                em2.style.border = "red solid 3px";
                document.getElementById("errEm").innerHTML = "invalid";
            }

            // pwd validation
            const pw1 = document.getElementById("pw").value;
            const pw2 = document.getElementById("pw");
            const re_pw = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[a-zA-Z]).{8,}$/;
            if (re_pw.test(pw1)) {
                pw2.style.border = "green solid 3px";
                document.getElementById("errPw").innerHTML = ":)";
            }
            else {
                pw2.style.border = "red solid 3px";
                document.getElementById("errPw").innerHTML = "invalid";
            }
        }
    </script>
    <script type="text/javascript">
        function preback(){
            window.history.forward()
        }
         setTimeout("preback()",0);
         window.onunload=function(){null};
    </script>
    <style>
        .cont {
            font-weight: bold;
            margin-top: 175px;
            margin-left: 45%;
            font-family: Arial, Helvetica, sans-serif;
            align-self: center;
            size: 5cm;
            height: 200px;
            font-size: 14pt;
        }

        .loginbtn {
            background-color: rgb(78, 78, 244);
            color: whitesmoke;
            height: 24px;
            font-size: 14pt;
        }

        .loginbtn:hover {
            opacity: 1;
            background-color: rgb(15, 15, 91);
            border-radius: 2px solid black;
        }
    </style>
</head>

<body>
    <div class="cont">
        <table>
            <tr>
                <td>
                    <form action="/success" method="post" onsubmit="validate()">
                        <hr>
                        <label for="emal">Registered Email</label><br>
                        <input type="email" id="em" class="box" placeholder="Enter email" name="email"
                            autocomplete="off"><br><br>

                        <label for="pwd">Password</label><br>
                        <input type="password" id="pw" class="box" placeholder="Enter password" name="pwd"
                            autocomplete="off"><br><br>

                        <input class="loginbtn" class="box" type="submit" value="Log in">
                        <hr>
                </td>
            </tr>
            </form>
        </table>
    </div>
</body>

</html>

